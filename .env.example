# Contact form / Resend
RESEND_API_KEY=
CONTACT_FROM_EMAIL=Portfolio <hello@your-verified-domain.com>
CONTACT_TO_EMAIL=taiwoemmanuel693@gmail.com

# WebDesk AI — keep this server-side; never expose the real key in client code
AI_GATEWAY_API_KEY=

# Admin panel (set private values in Vercel/environment settings; never commit the real password/secret)
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=
